import { FiGithub, FiTwitter, FiLinkedin, FiInstagram, FiHeart } from 'react-icons/fi';
import { SiLeetcode } from 'react-icons/si';
import { Link } from 'react-router';

const socialLinks = [
	{ icon: <FiGithub />, url: 'https://github.com/shashikant2000', label: 'GitHub' },
	{ icon: <FiLinkedin />, url: 'https://www.linkedin.com/in/shashikantkumar2000/', label: 'LinkedIn' },
	{ icon: <FiTwitter />, url: 'https://x.com/Shashik46200028', label: 'Twitter' },
	{ icon: <SiLeetcode />, url: 'https://leetcode.com/u/shashikant18096/', label: 'LeetCode' },
	{ icon: <FiInstagram />, url: 'https://www.instagram.com/shashikant_k_y/', label: 'Instagram' },
];

const footerLinks = [
	{ to: '/projects', label: 'Projects' },
	{ to: '/about', label: 'About' },
	{ to: '/contact', label: 'Contact' },
	{ to: '/resume', label: 'Resume' },
];

const AppFooter = () => {
	return (
		<footer className="relative mt-32">
			{/* Gradient divider */}
			<div className="h-px bg-gradient-to-r from-transparent via-indigo-500/50 to-transparent" />

			<div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-16 sm:py-20">
				<div className="grid grid-cols-1 md:grid-cols-3 gap-12 md:gap-8">
					{/* Brand */}
					<div>
						<Link to="/" className="inline-block">
							<span className="font-general-bold text-2xl bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 bg-clip-text text-transparent">
								SK
							</span>
						</Link>
						<p className="mt-4 text-gray-500 dark:text-gray-400 font-general-regular text-sm max-w-xs">
							Software Engineer at Microsoft, building tools that help developers move to the cloud.
						</p>
					</div>

					{/* Quick links */}
					<div>
						<h3 className="font-general-medium text-sm uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-4">
							Navigation
						</h3>
						<ul className="space-y-3">
							{footerLinks.map((link) => (
								<li key={link.to}>
									<Link
										to={link.to}
										className="text-gray-600 dark:text-gray-300 hover:text-indigo-500 dark:hover:text-indigo-400 font-general-regular text-sm transition-colors duration-200"
									>
										{link.label}
									</Link>
								</li>
							))}
						</ul>
					</div>

					{/* Social */}
					<div>
						<h3 className="font-general-medium text-sm uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-4">
							Connect
						</h3>
						<div className="flex flex-wrap gap-3">
							{socialLinks.map((link) => (
								<a
									key={link.label}
									href={link.url}
									target="_blank"
									rel="noopener noreferrer"
									aria-label={link.label}
									className="p-2.5 rounded-xl bg-gray-100 dark:bg-white/5 text-gray-500 dark:text-gray-400 hover:bg-indigo-50 dark:hover:bg-indigo-500/10 hover:text-indigo-500 dark:hover:text-indigo-400 transition-all duration-300 hover:-translate-y-0.5"
								>
									<span className="text-lg">{link.icon}</span>
								</a>
							))}
						</div>
					</div>
				</div>

				{/* Bottom bar */}
				<div className="mt-16 pt-8 border-t border-gray-200/50 dark:border-white/5">
					<div className="flex flex-col sm:flex-row justify-between items-center gap-4">
						<p className="text-sm text-gray-500 dark:text-gray-400 font-general-regular flex items-center gap-1">
							&copy; {new Date().getFullYear()} Built with
							<FiHeart className="w-3.5 h-3.5 text-red-500" />
							by Shashikant Kumar
						</p>
						<p className="text-xs text-gray-400 dark:text-gray-500 font-general-regular">
							React &bull; Vite &bull; Tailwind CSS
						</p>
					</div>
				</div>
			</div>
		</footer>
	);
};

export default AppFooter;
